name: TypeScript Lint and Reviewdog

on:
    pull_request:
        branches:
            - main

permissions:
    contents: read
    pull-requests: write

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: "20.x"

            - name: Install ESLint for development
              run: npm install --save-dev eslint @eslint/js @types/eslint__js typescript typescript-eslint @typescript-eslint/parser

            - name: Run ESLint with Reviewdog
              uses: reviewdog/action-eslint@v1
              with:
                  reporter: github-pr-review
                  eslint_flags: "-c eslint.config.mjs **/*.{ts,tsx}"
                  level: error
              env:
                  REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
